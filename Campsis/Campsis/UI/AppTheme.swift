import SwiftUI

extension Color {
    static let chatAccent = Color.blue
    static let assistantAccent = Color.purple
    static let userBubbleBackground = Color.blue.opacity(0.1)
    static let assistantBubbleBackground = Color(.controlBackgroundColor)

    static let sourceText = Color.blue
    static let sourceScreenshot = Color.purple
    static let sourceNote = Color.orange
    static let sourceVoice = Color.green
    static let sourceFile = Color.gray

    static let tagBackground = Color.blue.opacity(0.1)
}
