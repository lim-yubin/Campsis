# Campsis — AI Personal Memory for macOS

당신이 보고, 읽고, 생각한 모든 것을 기억합니다.
Campsis는 macOS에서 동작하는 로컬 우선 AI 개인 메모리 앱입니다.

---

## 핵심 가치

- **로컬 우선** — 모든 데이터는 기기에 저장됩니다. 검색·임베딩은 온디바이스에서 수행됩니다.
- **제로 마찰** — 단축키 하나로 현재 맥락을 즉시 기억합니다.
- **프라이버시** — 답변 생성 시에도 전체 데이터가 아닌, 질문과 관련된 기억 조각만 전송됩니다.

---

## 기능

### 스마트 캡처

어떤 앱에서든 텍스트를 선택하고 단축키 하나(`⌥ Space`)로 기억할 수 있습니다.
텍스트가 선택되지 않은 상태라면, 현재 보고 있는 화면을 자동으로 스크린샷합니다.

- 텍스트 선택 감지 (AX API + 클립보드 폴백)
- 텍스트 미선택 시 자동 스크린샷 전환
- Chrome, Safari, Slack, Cursor, Terminal 등 주요 앱 지원

### 컨텍스트 인식

캡처 시점의 맥락을 자동으로 함께 저장합니다.
나중에 "그때 어디서 봤더라?"를 기억해낼 수 있습니다.

- 현재 사용 중인 앱 이름
- 윈도우 제목
- 브라우저 URL (Chrome, Safari, Arc 등)
- 마우스 커서 위치 기준 — 듀얼 모니터에서도 정확히 보고 있던 앱을 인식

### 캡처 팝업

캡처된 내용을 즉시 미리보기로 확인하고, 메모를 추가할 수 있습니다.

- 선택한 텍스트 또는 스크린샷 미리보기 — 스크린샷은 크게, 긴 텍스트는 스크롤로 확인
- 출처 정보 (앱, 윈도우 제목) 표시
- 자유 메모 입력 필드
- 팝업 크기 조절 가능 — 모서리를 드래그해 편한 크기로, 미리보기도 함께 확대
- ⌘↩로 저장, Esc로 취소 (메모를 여러 줄 입력해도 저장 단축키와 충돌 없음)
- 마우스가 있는 모니터에 팝업 표시
- 저장 완료 시 "기억함" 피드백 표시

### 메뉴바 앱

메뉴바에 조용히 상주하거나, Dock 앱으로 쓸 수 있습니다. 원하는 방식으로 설정하세요.

- 시스템 메뉴바 아이콘
- 빠른 메모 / 기억 열기 / 설정 / 종료 메뉴 (전부 한국어)
- AI 준비 상태 표시 — 다운로드 진행률(%) / 로딩 중 / 준비됨
- Dock 표시 여부 선택 가능 — 메뉴바 전용 또는 Dock 앱으로 동작

**Dock 앱 모드에서도 완결된 경험:**
- 상단 앱 메뉴 — 새 채팅(⌘N), 빠른 메모(⌘⇧N), 기억 열기(⌘⇧M)
- Dock 아이콘 우클릭 메뉴 — 빠른 메모 / 새 채팅 / 기억 열기
- 창 사이드바 하단에 AI 상태와 설정 진입점 — 메뉴바를 열지 않아도 확인
- Dock 아이콘을 다시 누르면 창을 앞으로 가져옴

### Quick Note

`⌥⇧ Space` 하나로 떠오르는 생각을 바로 기록합니다.

- 키보드 단축키로 즉시 메모 팝업
- 여러 줄 텍스트 입력
- 저장 즉시 AI가 요약 + 태깅 + 검색 인덱싱
- 아이디어, 할 일, 대화 내용 등 무엇이든

**기술:** NSPanel, SwiftUI TextEditor, ProcessingQueue 자동 연동

### 파일 가져오기

PDF, 텍스트, 이미지 파일을 드래그하거나 선택해서 기억에 추가합니다.

- 앱 창에 파일 드래그 앤 드롭
- 또는 + 버튼으로 파일 선택
- PDF: 텍스트 자동 추출 → 검색 가능
- 이미지: Luna 비전이 읽고 이해 → 정리본 생성
- TXT/Markdown: 전체 내용 저장

**기술:** PDFKit, UniformTypeIdentifiers, fileImporter

### 커스텀 단축키

기본 단축키가 불편하면 원하는 키 조합으로 변경할 수 있습니다.

- 설정 창에서 단축키 직접 녹화
- 기본값 복원 버튼
- 현재 컨텍스트 기억: `⌥ Space`
- 빠른 메모: `⌥⇧ Space`
- 기억 열기: `⌥⇧ M`
- 최초 실행 시 온보딩으로 권한과 단축키를 단계별 안내

### AI 자동 처리

캡처한 내용을 AI가 자동으로 분석합니다. 별도 조작 없이, 저장하는 순간 백그라운드에서 처리됩니다.

- 이미지 통합 이해 — 스크린샷·이미지를 Luna가 그대로 읽고 해석해 정리본 생성 (단순 글자 추출을 넘어 화면·문서·차트의 의미까지 파악)
- 내용 자동 요약 — "이건 무슨 내용이었지?" 할 때 한눈에 파악
- 주제 자동 태깅 — AI가 관련 키워드를 붙여줌
- 편집 가능한 정리본(Markdown) 생성 — 캡처한 내용을 지식 노트로 정리, 언제든 직접 수정 가능
- 벡터 임베딩 자동 생성 — 의미 기반 검색을 위한 사전 처리 (온디바이스 bge-m3)
- 하이브리드 처리 — 임베딩·검색은 기기 안에서, "정리/이해"는 Luna가 담당
- 실패 시 자동 재시도 (최대 3회)

### 대화형 기억 검색 (Chat)

ChatGPT처럼 대화하며 기억을 검색합니다. 후속 질문도 가능합니다.

- 자연어 질문으로 기억 검색 — "그거 뭐였지?"
- 실시간 스트리밍 답변 — 글자가 생성되는 대로 즉시 표시, "생각 중" 표시
- 생성 중지 — 답변이 마음에 안 들면 언제든 중지
- 대화형 UI — 이전 대화 맥락을 이어서 후속 질문
- 대화 제목 자동 생성 — AI가 대화 주제를 2~4단어로 요약
- 대화 이력 저장 — 앱을 다시 열어도 이전 대화 확인 가능
- 의미 기반 벡터 검색 — 단어가 정확히 일치하지 않아도 찾음 (관련성 낮은 결과는 자동 제외)
- 깔끔한 답변 — 본문에 `[1]` 같은 인용 번호 없이 자연스럽게, 근거 출처는 말풍선 아래에만 표시
- 상황에 맞는 형식 — 기억을 근거로 한 답변은 표·목록까지 갖춘 정돈된 형태로, 가벼운 대화는 평범한 채팅 문장으로
- 넓게 보는 답변 — 답변은 화면 전체 폭을 사용해 표·목록도 시원하게, 복사 버튼은 답변 우측 상단에
- 관련 있는 출처만 — 관련도가 높은 기억만 골라 보여줌 (무의미하게 5개씩 나열하지 않음)
- 위키 우선 답변 — 같은 주제로 정리된 위키가 있으면 그 종합 문서를 먼저 근거로 삼고, 세부는 개별 메모로 보완
- 출처를 항상 칩으로 노출 — 답변 아래에 근거가 관련도순 칩으로 늘 보이고, 각 칩에 `위키`/`메모` 배지로 종류를 구분 (많으면 "+N개 더보기")
- 출처 클릭 → 바로 이동 — 메모 칩은 오른쪽 패널에 정리본을 미리보기(지금 보는 출처는 하이라이트), 위키 칩은 "나의 위키"에서 해당 위키 상세로 이동 (이미지는 캡처본이 상단에 표시)
- 미리보기에서 바로 전체 보기 — 미리보기 상단의 "전체 보기 / 원본 / 원문 열기"로 편집 가능한 상세 화면이나 원본까지 한 번에 이동
- 기억 + 채팅 동시 보기 — 어느 화면에서든 툴바 토글로 오른쪽 패널에서 전체 기억을 탐색하며 대화 (패널 폭 자유 조절)
- 채팅 출처를 옆에서 바로 확인 — 답변 아래 출처 칩을 누르면 대화를 떠나지 않고 오른쪽 패널에서 정리본·위키를 미리봅니다 (위키는 "나의 위키에서 열기"로 전체 화면 이동)
- 정리본 복사 — 마크다운 원문 복사 / 서식 없는 일반 텍스트 복사 선택 가능
- 편안한 입력 — Enter로 전송, Shift+Enter로 줄바꿈, 입력창은 내용에 따라 자동으로 늘어남
- 진입 즉시 최근 대화 — 채팅방을 열면 가장 최근 메시지가 바로 보임
- 여러 대화 관리 — 주제별로 새 채팅 시작 가능
- 컨텍스트가 길어지면 "새 채팅 시작"을 친절히 안내

**기술:** GPT-5.6 Luna (클라우드), VectorSearch(bge-m3, 온디바이스), SQLite 대화 저장

### AI 엔진

답변 생성은 클라우드 AI(GPT-5.6 Luna)가 담당하고, 검색과 저장은 언제나 기기 안에서 이뤄집니다. 무거운 로컬 모델을 기기에 두지 않아 앱이 가볍고 답변 품질이 높습니다.

- 클라우드 AI (GPT-5.6 Luna) — 높은 답변 품질, 낮은 기기 부담, 앱 용량 최소화
- 내 키 연결(BYOK) — 본인 OpenAI 키를 설정에 입력하면 그 키로 동작
- 키는 macOS 키체인에 안전하게 보관 — 앱에 심지 않음
- 클라우드로는 전체 데이터가 아닌, 질문과 관련된 기억 조각만 전송

**기술:** ChatEngineProtocol 추상화(로컬 MLX ↔ 클라우드 Luna 런타임 전환), Keychain, AICredentials 계층 키 관리

### 지식 노트(정리본)

캡처한 내용을 AI가 읽기 좋은 마크다운 노트로 정리해 둡니다. 나중에 다시 볼 때 원본을 훑을 필요 없이, 핵심만 구조화된 노트로 확인할 수 있습니다.

- 제목·요약·핵심 포인트·태그로 자동 구조화된 마크다운 노트
- 한눈에 알아보는 제목 — AI가 붙인 간결한 제목으로 기억 목록을 정리(원문 조각이 아니라 의미가 보이는 제목)
- **표·링크·이미지까지 제대로 렌더링** — 표는 격자로, 링크는 눌러서 열고, 로컬 이미지는 바로 노트 안에 표시
- 캡처는 즉시 저장·검색 가능, 정리본은 온라인일 때 백그라운드에서 조용히 생성
- 정리본을 만들 때 참고한 **원본 소스를 바로 옆 '원본' 탭에서 즉시 확인**
- 노트는 앱 폴더에 `.md` 파일(진실원)로 보관 — 사람이 읽고 편집할 수 있는 형태
- **정리본을 편집하면 검색에도 즉시 반영** — 편집한 노트 기준으로 해당 항목만 다시 색인
- 로컬 전용 모드에서는 정리본 생성을 건너뛰고 원본만 보관

**기술:** MarkdownGenerator 추상화(GPT-5.6 Luna 백엔드), MD 파일 진실원 + 벡터 파생 인덱스(편집 시 항목 단위 재임베딩), 백그라운드 지연 생성

### 나의 위키

흩어진 메모를 주제별로 **하나의 정리된 문서**로 묶어 줍니다. 폴더처럼 모아만 두는 게 아니라, 같은 주제의 메모들을 AI가 **종합해 한 편의 위키 페이지로 다시 써 줍니다.** 메모가 쌓일수록 위키가 스스로 더 풍부해집니다.

- 원할 때만 정리 — 메모함에서 원하는 정리본을 체크하고 "위키에 정리"를 누르면 됩니다 (평소 캡처는 그대로 무마찰)
- 똑똑한 목적지 추천 — 고른 메모가 기존 위키 중 어디에 어울리는지 자동으로 찾아 제시하고, 없으면 새 위키를 제안합니다. 원하면 목적지를 직접 바꾸거나 여러 위키에 함께 넣을 수 있습니다
- 종합해서 다시 쓰기 — 단순히 이어붙이지 않고, 기존 위키에 새 메모를 녹여 하나의 일관된 문서로 재구성합니다 (사실에 충실, 상충되는 내용은 표시)
- 한곳에서 보기 — 위키 상세에서 종합 요약·핵심 포인트, 이 위키가 어떤 메모들로 만들어졌는지, 관련된 다른 위키(백링크)를 함께 확인
- 알아서 이어지는 관련 위키 — 제목이나 키워드가 달라도 **내용이 가까운 위키끼리 자동으로 "관련 위키"로 연결**됩니다. 위키가 갱신될 때 자연스럽게 이어지고, "나의 위키" 상단 "관련 위키 다시 잇기" 버튼을 누르면 전체 위키의 관계를 한 번에 다시 계산합니다 (추가 AI 호출 없이 즉시)
- 구성 메모 되짚기 — 위키를 이루는 메모를 눌러 원래 정리본을 바로 옆에서 확인
- 안전한 편집 — 위키 상세에서 "편집"으로 페이지를 직접 다듬을 수 있고, 저장하면 "직접 편집" 상태가 되어 이후 새 메모를 정리해도 그 내용을 자동으로 덮어쓰지 않습니다. 원하면 "자동 정리 다시 켜기"로 되돌릴 수 있고, 정리 전 버전은 조용히 백업됩니다
- 스스로 깔끔해지는 위키 — 비슷한 위키가 겹치거나 내용이 빈약한 위키가 생기면 "정리 제안" 카드로 알려줍니다. 버튼 한 번으로 합치거나 삭제할 수 있고(강제 아님), 원치 않으면 무시하면 한동안 다시 뜨지 않습니다

**기술:** 정리본 임베딩 매칭(bge-m3, 코사인+태그 가중, 임계값 2단 밴드) + 새 위키 제안, 증분 재합성(현재 페이지+새 메모만 Luna 전달로 비용 선형), 위키 페이지 임베딩·백링크(wiki_wiki_link, 출처별 kind 구분), 유사도 기반 관련 위키 자동 연결(위키 임베딩 코사인, 재임베딩 시 자동 + 수동 전체 재계산 버튼, AI 호출 없음), MD 스냅샷 되돌리기, 유지보수 Lint(중복·빈약 위키 로컬 감지, AI 호출 없음, 비파괴 제안 카드 + 무시 쿨다운)

### 소스 상세 보기

기억 목록에서 항목을 누르면 팝업이 아니라 **화면 안에서 자연스럽게 열립니다**. 정리본이 먼저 보이고, 필요하면 바로 편집하거나 원본을 확인할 수 있습니다.

- 정리본(MD)이 주 화면 — AI가 정리한 노트를 먼저 확인, 원본은 "원본 보기 (N자)" 보조 토글로 필요할 때만 펼침
- **원본 수정 → 정리본 재생성이 기본 편집 경로** — 텍스트·메모·파일 원본을 직접 고치면 AI가 정리본을 다시 만들고 검색 색인까지 갱신 (이미지 제외). 직접 다듬은 정리본이 있으면 덮어쓰기 전에 확인
- 정리본 직접 손질은 "고급: 표현만 다듬기" — 내용이 아니라 문장 표현만 손볼 때 사용 (⌘S로 저장), 정리본이 없으면 "직접 작성"으로 새로 작성
- 섹션별 복사 버튼 — 정리본·요약·OCR·전사·원본 내용을 각 섹션에서 바로 복사, "복사되었습니다" 토스트로 일관된 피드백
- AI 요약 + 주제 태그
- 출처 메타데이터 (앱, URL, 날짜)

### 기억 관리 (Memories)

저장된 모든 기억을 한눈에 관리합니다.

- 3섹션 사이드바 — 기억(전체 기억·오늘) / 위키(나의 위키) / 대화로 깔끔하게 구분해 탐색
- 유형으로 걸러 보기 — 텍스트·스크린샷·메모·음성·파일 유형 필터가 사이드바 대신 메모함 상단 "유형" 메뉴로 이동해 목록이 간결해짐
- 의미로 찾는 검색 — 상단 검색창은 단어가 정확히 일치하지 않아도 뜻이 비슷한 기억까지 함께 찾아줌 (정확 일치 우선 + 의미 유사 병합)
- 위키까지 한 번에 검색 — 검색하면 관련된 "나의 위키" 종합 문서가 결과 맨 위에 함께 뜨고, 누르면 바로 위키 상세로 이동
- 검색어로 바로 채팅 — 검색 결과 상단의 "‘…’로 채팅하기"를 누르면 그 검색어가 새 채팅 입력창에 채워진 채 열림
- 한눈에 보이는 목록 — 각 항목에 한 줄 요약 스니펫·주제 태그·스크린샷 썸네일이 함께 표시되고, 지금 보고 있는 항목은 하이라이트
- 시간순 목록 — 최신 기억부터 날짜별로 확인
- 날짜 필터 — 달력에서 특정 날짜의 기억만 보기
- 처리 상태 확인 — 처리 중/완료/실패 표시
- 소스 상세에서 "원문 열기" — 저장한 URL/파일을 바로 재방문
- 대화는 사이드바 "대화" 섹션에서 관리 — 사이드바 상단의 작성 아이콘으로 새 채팅 시작
- 사이드바 상단 토글로 접기/펼치기 — 접어도 작성·토글 아이콘은 그대로 유지
- 사이드바 너비 240 고정 — 일관된 레이아웃으로 콘텐츠 영역을 최대한 확보

### 로컬 저장소

모든 데이터는 SQLite 데이터베이스에 안전하게 로컬 저장됩니다.

- `~/Library/Application Support/Campsis/` 경로
- 텍스트, 스크린샷, 메모, 메타데이터 통합 관리
- 스크린샷은 PNG 파일로 별도 저장

---

## 기술 스택

| 영역 | 기술 |
|------|------|
| 플랫폼 | macOS 26+, Apple Silicon |
| 언어 | Swift 6 |
| UI | SwiftUI + AppKit (NSPanel) |
| 데이터베이스 | SQLite via GRDB.swift |
| 스크린 캡처 | ScreenCaptureKit |
| 텍스트 감지 | Accessibility API (AX) |
| 이미지 이해 / 정리 | GPT-5.6 Luna 비전 (OCR·요약·태깅·정리본 단일 처리) |
| AI 답변 생성 | GPT-5.6 Luna (클라우드, BYOK) |
| 임베딩 | Core ML (bge-m3 fp16, 1024-dim) |
| 토크나이저 | swift-transformers (XLM-RoBERTa) |
| 벡터 검색 | Accelerate framework (vDSP) |
| 단축키 | KeyboardShortcuts 라이브러리 |
| 배포 | Developer ID + Notarization |

---

## 로드맵

| Phase | 내용 | 상태 |
|-------|------|------|
| Phase 0 | 임베딩 모델 평가 | 완료 |
| Phase 1 | 캡처 기반 구축 | 완료 |
| Phase 2 | AI 자동 처리 (이해, 요약, 태깅, 임베딩) | 완료 |
| Phase 3 | 검색 + Recall + 기억 관리 | 완료 |
| Phase 3.5 | ~~MLX LLM 통합 (Qwen3-4B)~~ 제거됨 (클라우드 Luna로 일원화) | 완료 |
| Phase 4 | Quick Note + 파일 가져오기 | 완료 |
| Phase 4.5 | 대화형 UI (Chat) | 완료 |
| Phase 5.5 | UX 개선 1차 (스트리밍, 온보딩, 검색 등) | 완료 |
| Phase 7 | 클라우드 생성(Luna) 일원화 + MD 지식베이스 (로컬 LLM 제거) | 진행중 |
| Phase 5 | 실사용 검증 + 최적화 | 진행중 |
| Phase 6 | 릴리스 + 배포 (브라우저 다운로드 + 웹 대시보드) | 예정 |
