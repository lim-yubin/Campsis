# Campsis — AI Personal Memory for macOS

당신이 보고, 읽고, 생각한 모든 것을 기억합니다.
Campsis는 macOS에서 동작하는 로컬 우선 AI 개인 메모리 앱입니다.

---

## 핵심 가치

- **로컬 우선** — 모든 데이터는 기기에 저장됩니다. 클라우드 업로드 없음.
- **제로 마찰** — 단축키 하나로 현재 맥락을 즉시 기억합니다.
- **프라이버시** — Apple Silicon에서 모든 AI가 온디바이스로 동작합니다.

---

## 기능

### 스마트 캡처

어떤 앱에서든 텍스트를 선택하고 단축키 하나(`⌥ Space`)로 기억할 수 있습니다.
텍스트가 선택되지 않은 상태라면, 현재 보고 있는 화면을 자동으로 스크린샷합니다.

- 텍스트 선택 감지 (AX API + 클립보드 폴백)
- 텍스트 미선택 시 자동 스크린샷 전환
- Chrome, Safari, Slack, Cursor, Terminal 등 주요 앱 지원

### 컨텍스트 인식

캡처 시점의 맥락을 자동으로 함께 저장합니다.
나중에 "그때 어디서 봤더라?"를 기억해낼 수 있습니다.

- 현재 사용 중인 앱 이름
- 윈도우 제목
- 브라우저 URL (Chrome, Safari, Arc 등)
- 마우스 커서 위치 기준 — 듀얼 모니터에서도 정확히 보고 있던 앱을 인식

### 캡처 팝업

캡처된 내용을 즉시 미리보기로 확인하고, 메모를 추가할 수 있습니다.

- 선택한 텍스트 또는 스크린샷 미리보기
- 출처 정보 (앱, 윈도우 제목) 표시
- 자유 메모 입력 필드
- Enter로 저장, Esc로 취소
- 마우스가 있는 모니터에 팝업 표시

### 메뉴바 앱

Dock을 차지하지 않고, 메뉴바에 조용히 상주합니다.
필요할 때만 단축키로 호출하세요.

- 시스템 메뉴바 아이콘
- Quick Memory / Open Memory / Settings / Quit 메뉴
- Dock 아이콘 없음 — 깔끔한 작업 환경 유지

### 커스텀 단축키

기본 단축키가 불편하면 원하는 키 조합으로 변경할 수 있습니다.

- Settings 창에서 단축키 직접 녹화
- 기본값 복원 버튼
- Remember Context: `⌥ Space`
- Quick Memory: `⌥⇧ Space`

### AI 자동 처리

캡처한 내용을 AI가 자동으로 분석합니다. 별도 조작 없이, 저장하는 순간 백그라운드에서 처리됩니다.

- 스크린샷 속 텍스트 자동 추출 (OCR)
- 내용 자동 요약 — "이건 무슨 내용이었지?" 할 때 한눈에 파악
- 주제 자동 태깅 — AI가 관련 키워드를 붙여줌
- 벡터 임베딩 자동 생성 — 의미 기반 검색을 위한 사전 처리
- 모든 처리는 온디바이스 — 데이터가 기기 밖으로 나가지 않음
- 실패 시 자동 재시도 (최대 3회)

### 기억 검색 (Recall)

자연어로 질문하면, 저장된 기억에서 답을 찾아줍니다.

- "그거 뭐였지?" — 자연어 질문으로 기억 검색
- 의미 기반 벡터 검색 — 단어가 정확히 일치하지 않아도 관련 내용을 찾음
- 관련 소스 즉시 표시 — 검색 결과를 먼저 보여주고, AI 답변은 이후 생성
- AI 답변 생성 — 관련 소스를 종합하여 자연어 답변
- 근거 기반 답변 — 답변에 사용된 소스를 인용으로 표시
- 근거 부족 시 솔직한 거절 — "저장된 Memory에서 찾지 못했습니다"

### 소스 상세 보기

저장된 기억 하나하나를 상세하게 확인할 수 있습니다.

- 원본 텍스트/스크린샷 전체 보기
- OCR 추출 텍스트 확인
- AI 요약 + 주제 태그
- 출처 메타데이터 (앱, URL, 날짜)

### 기억 관리 (Memories)

저장된 모든 기억을 한눈에 관리합니다.

- 시간순 목록 — 최신 기억부터 확인
- 타입별 필터 — 텍스트, 스크린샷, 메모, 음성, 파일
- 처리 상태 확인 — 처리 중/완료/실패 표시

### 로컬 저장소

모든 데이터는 SQLite 데이터베이스에 안전하게 로컬 저장됩니다.

- `~/Library/Application Support/Campsis/` 경로
- 텍스트, 스크린샷, 메모, 메타데이터 통합 관리
- 스크린샷은 PNG 파일로 별도 저장

---

## 기술 스택

| 영역 | 기술 |
|------|------|
| 플랫폼 | macOS 26+, Apple Silicon |
| 언어 | Swift 6 |
| UI | SwiftUI + AppKit (NSPanel) |
| 데이터베이스 | SQLite via GRDB.swift |
| 스크린 캡처 | ScreenCaptureKit |
| 텍스트 감지 | Accessibility API (AX) |
| OCR | Apple Vision Framework |
| AI 분석 | Apple Foundation Models (@Generable) |
| 임베딩 | Core ML (bge-m3 fp16, 1024-dim) |
| 토크나이저 | swift-transformers (XLM-RoBERTa) |
| 벡터 검색 | Accelerate framework (vDSP) |
| 단축키 | KeyboardShortcuts 라이브러리 |
| 배포 | Developer ID + Notarization |

---

## 로드맵

| Phase | 내용 | 상태 |
|-------|------|------|
| Phase 0 | 임베딩 모델 평가 | 완료 |
| Phase 1 | 캡처 기반 구축 | 완료 |
| Phase 2 | AI 자동 처리 (OCR, 요약, 태깅, 임베딩) | 완료 |
| Phase 3 | 검색 + Recall + 기억 관리 | 완료 |
| Phase 3.5 | 고급 AI (Reranker, 조건부) | 대기 |
| Phase 4 | Quick Memo + 음성 메모 | 예정 |
| Phase 5 | 실사용 검증 + 최적화 | 예정 |
| Phase 6 | 릴리스 + 배포 | 예정 |
